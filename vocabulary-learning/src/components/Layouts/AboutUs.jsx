import PropTypes from 'prop-types';

const AboutUs = props => {
    return (
        <div>
            this is about us page
        </div>
    );
};

AboutUs.propTypes = {
    
};

export default AboutUs;